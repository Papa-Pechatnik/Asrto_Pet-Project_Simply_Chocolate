---
import { Image } from "astro:assets";
import poster from "/src/assets/images/video-poster.png";
import iconPlay from "/src/assets/icon/icon-video-play.svg";
---

<section class="production section-bottom">
  <div class="container">
    <h2 class="text-title text-h2 text-center">how it’s<span class="text-accent"> made?</span></h2>
    <div class="production-wrap">
      <div class="production-video">
        <video class="production-video__video" src="" controls></video>
        <Image
          class="production-video__img"
          src={poster}
          alt="poster"
          widths={[320, 600, 600]}
          sizes="(max-width: 375px) 100vw, (max-width: 767px) 100vw, (min-width: 767.02px) 50vw"
          quality={"80"}
        />
        <Image class="production-video__play" src={iconPlay} width={44} width={44} alt="icon-play"/>
      </div>
      <section class="production-content">
        <p class="production-content__title">Try our chocolates today and discover the perfect balance of flavor</p>
        <ul class="content-list">
          <li class="text">Heat the milk in a saucepan without bringing it to a boil. Add sugar and cocoa powder.</li>
          <li class="text">At the same time, prepare a water bath. Melt the butter on it.</li>
          <li class="text">
            "Mix" the milk mixture and already liquid butter. Mix thoroughly with a silicone spatula so that the mass
            becomes uniform and silky.
          </li>
          <li class="text">Heat the mixture over low heat, letting it barely boil.</li>
          <li class="text">
            Pour the chocolate into special containers (can be replaced with ice molds). Allow to cool and harden.
          </li>
        </ul>
      </section>
    </div>
  </div>
</section>

<style>
  .production .text-title {
    margin-bottom: var(--indent-50-32);
  }

  .production-wrap {
    display: flex;
    gap: var(--gap-tertiary);
  }

  .production-video {
    position: relative;
    max-width: 42.8571rem;
    min-width: 33.5714rem;
    width: 100%;

    height: 400px;
    border-radius: var(--radius-15);
  }

  .production-video__video {
    width: 100%;
    height: 100%;
    border-radius: inherit;
  }

  .production-video__img {
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
    border-radius: inherit;
    object-fit: cover;
  }

  .production-video__play {
    position: absolute;
    width: 44px;
    height: 44px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .production-content {
    max-width: 36.8571rem;
    width: 100%;
    background-color: var(--color-secondary);
    padding: clamp(1.429rem, 0.542rem + 3.183vw, 2.286rem) clamp(1rem, -0.33rem + 4.775vw, 2.286rem);
    border-radius: 1.071rem;
  }

  .production-content__title {
    max-width: 24rem;
    font-family: var(--font-family-primary);
    font-weight: 600;
    font-size: 1.2857rem;
    line-height: 133%;
    color: var(--color-primary);
  }

  .content-list {
    max-width: 30.3571rem;
    margin-top: var(--indent-28-20);
    display: flex;
    flex-direction: column;
    row-gap: clamp(1.286rem, 0.527rem + 1.386vw, 1.714rem); /*1200 - 24 767 - 18*/
    list-style: disc;
    list-style-position: outside;
  }

  @media screen and (max-width: 1023.98px) {
    .production-wrap {
      flex-direction: column;
      align-items: center;
    }

    .production-video,
    .production-content {
      max-width: 43.8571rem;
      min-width: auto;
    }

    .content-list  {
      max-width: 34.2857rem;
    }

  }
</style>
